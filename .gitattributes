# 统一换行符：仓库里一律存 LF，Windows 检出时脚本仍按各自规则处理
* text=auto eol=lf

# Windows 批处理必须是 CRLF
*.bat text eol=crlf

# Shell 脚本必须是 LF，否则 Linux 上会报 "bad interpreter"
gradlew text eol=lf

# 二进制文件不要做换行转换
*.jar binary
*.png binary
*.ico binary
